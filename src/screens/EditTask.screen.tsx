import React from 'react';
import {Text, View} from 'react-native';

import {EditTaskProps} from '../types';

export default function EditTask({navigation}: EditTaskProps) {
  return (
    <View>
      <Text>EditTask</Text>
    </View>
  );
}
